{{page-title "Besluiten"}}

<AuHeading @level="3" @skin="3">Suggestions</AuHeading>
  <ul>
    {{#each @model as |suggestion|}}
      <AuCard>
        <AiSuggestion @suggestion={{suggestion}} />
      </AuCard>
    {{/each}}
  </ul>
{{outlet}}